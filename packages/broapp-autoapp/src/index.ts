/**
 * `broapp-autoapp` — the launcher, the renderer, the specification and the
 * engineer.
 *
 * Empty for now. The IPC message types under `src/ipc/` are the first thing
 * that will be exported from here, once the launcher in prompt 05 has a
 * supervisor to put behind them.
 */
export {};
