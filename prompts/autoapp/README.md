# Broapp Autoapp — build prompts

Autoapp is the pivot: a Broapp application that its owner can reshape while
using it, with an engineer built into the experience. It lives on the
`autoapp` branch and in a new package, `packages/broapp-autoapp`. The core
`packages/broapp` is touched only for the execution gate and the `effect`
field on the contract. Nothing lands on `main` until every stage below has
cleared.

The prompts below, run in order, one per agent session. Each prompt is
self-contained but assumes the previous ones landed. Every prompt ends by
writing a short report to `prompts/autoapp/reports/NN-<name>.md`; the next
prompt starts by reading the reports so far.

| # | File | Produces | Gate |
|---|------|----------|------|
| 00 | `00-common-rules.md` | Rules and fixed decisions every prompt must follow. Read first, every time. | — |
| 01 | `01-execution-gate.md` | `effect` on the contract; release identity; one execution gate every entry point uses; adversarial tests. | `bun run check` green. `tests/ai-chat.test.ts` unmodified and green. |
| 02 | `02-spike-supervision.md` | Proof that a compiled launcher can supervise a compiled child over IPC and that the child can load a versioned app artifact with no Bun installed. | Stop if the spike fails. Report the failure. Do not continue to 03. |
| 03 | `03-app-spec-and-manifest.md` | The versioned application specification: manifest, contract export, view/workflow/migration references, capabilities. Spec store on disk. | Tests green. |
| 04 | `04-view-spec-and-renderer.md` | The declarative view specification and the pinned React renderer. Notes runs on it. Overrides by stable id. | Notes runs on the renderer. Tests green. |
| 05 | `05-candidate-and-activation.md` | Candidate releases, SQLite snapshots, drain, migrate-a-copy, health check, durable switch, activation journal, restart recovery. Failure tests. | Activation tests green, including interruption. |
| 06 | `06-run-store-and-workflows.md` | Supervisor-owned run store; runs, steps, unknown outcomes; save-as-workflow; promote a workflow to a view action. | Tests green. |
| 07 | `07-engineer-service.md` | The AI engineer: tools that read the spec, propose a change, build a candidate, preview it, and request activation. All through the gate. The Notes demo. | The full loop runs by hand. |
| 08 | `08-mcp-adapter.md` | An MCP server over the same gate, approvals answered from the Broapp tab, denied when no tab is attached. | Tests green. Manual check with an MCP client. |
| 08b | `08b-fixups.md` | Release identity covers the whole specification; hunk edits for the engineer; no `instanceof` across the release boundary; the demo rerun with a capable model. | Tests green. Demo log with one row per step. |
| 08c | `08c-engineer-usability.md` | Indentation-tolerant hunks, `apps.list`, a ten-minute confirm window for the engineer with a countdown. | Tests green. Demo step 3 rerun. |
| 09 | `09-packaging-matrix.md` | Linux and Windows coverage for the supervision chain; fixed packaged dependencies; the three offline tiers documented as tested. | CI green on every OS. |
| 10 | `10-phase-2-backlog.md` | Not a build prompt. What was deliberately deferred and why. | — |
| 11 | `11-new-application.md` | A starter workspace embedded in the launcher binary; `launcher.appCreate`, a `create` command and an `apps.create` tool sharing one implementation with `import`; a **New application** button in the tab. | Tests green. Smoke test's create step passes. |
| 12a | `12a-knowledge-foundations.md` | `knowledge.sqlite`: every build, check, edit and activation as a sanitised event with the run id, call id and source revision it had at the time; failures and repairs as immutable cases with the context as delivered; the candidate durable across a restart; `launcher.previewStart`. Five optional core hooks. | Tests green. `tests/ai-chat.test.ts` unchanged. |
| 12b | `12b-knowledge-path.md` | Orientation and task-evidence documents on every turn; `source.search`; curated facts as build-failure hints; `verification` on every edit result; servings with delivered-or-not and stage-aware outcomes. No model calls. | Tests green. 08c request rerun: tool calls before the first edit and whether a build was reached. |
| 12c | `12c-knowledge-distillation.md` | One structured question to the model per resolved case; six diagnoses; provisional lessons with provenance; supersession and review flags; `knowledge list/show/confirm/retire/export`; `Ai.model()`. | Tests green. One real distillation logged. |
| 12d | `12d-knowledge-replay.md` | Replay a case with and without its lesson from a manifest into fresh checkouts with a frozen corpus; `knowledge confirm` shows the table; `knowledge evaluate` runs four conditions on three tasks. | Tests green. Evaluation table beside the 08c numbers. |
| 12e | `12e-site-and-docs.md` | The site's header menu built from page groups so Autoapp is a tab; a landing page in `docs/autoapp/README.md` with the architecture diagram; titles without the prefix; the banner version from the manifest; nested relative links to `diagrams/` resolving. | Tests green. `bun run site` builds; the tab and the diagram visible. |
| 12f | `12f-theme-contract.md` | One table of theme tokens; the renderer's default stylesheet and a `theme` reference topic generated from it and held to it by tests; spacing, type, radius and density tokens at today's values; two presets and a gallery a person looks at; page size in `candidate.explain`. | Tests green. Gallery findings in the report. |
| 12g | `12g-component-compatibility.md` | The renderer's colour tokens read the seven application variables the AI panel reads, by meaning, from the token table; five type tokens replace the last literals; a headless-browser harness opens the compiled page under three themes and three schemes with the panel's select open and reads computed styles and contrast; the `theme` topic says how a style guide becomes a theme; the component strategy as a document and an authoring gate in the common rules. | Tests green. The harness table for nine combinations in the report. CI's `theme` job green. |
| 12h | `12h-design-guidance.md` | A `design` reference topic distilled from Impeccable's product register, every line naming a kind or a property the engineer can set, held to the kinds by a test; `PRODUCT.md` and `DESIGN.md` readable and named in the evidence, a three-question `PRODUCT.md` in the starter; the design check's count when the person is asked to look; Impeccable's detector in the theme CI job with its four contrast findings fixed; the skill's critique, audit and polish in the framework gate; attribution under Apache 2.0. | Tests green. The rendered topic and the detector's findings in the report. |
| 12i | `12i-remove-and-blank.md` | `launcher.appRemove` and a `remove` command that move an application to a trash the launcher never empties, after the person types its id; refused while serving; no engineer tool. **New application** offers the items starter or a blank page, the same choice on `create` and `apps.create`, starter still the default. | Tests green. Smoke creates one of each and removes one. |
| 12j | `12j-structured-history.md` | The host keeps each turn's model-form transcript under its run id and expands a history turn that names it, bounded, in place of the text; both clients name the run; the harness takes the same path; `knowledge evaluate` gains two-turn tasks under text and structured history, a restart cell and columns for repeated reads and actions. | Tests green. `tests/ai-chat.test.ts` unchanged. The two-turn table in the report. |
| 12k | `12k-knowledge-window.md` | A Knowledge panel in the tab: turns (what was delivered, cut or not, and each served lesson's outcome), lessons (servings, provenance, replay evidence) and cases; confirm, retire, write and replace a lesson from the tab through `review.ts`, which the CLI shares. | Tests green. The turns view described over the real store in the report. |
| 12m | `12m-panel-link.md` | Brobridge 0.2.2 mints more than one single-use launch address (Step 0, its own repository, then a stop for the publish); an **Autoapp** mark in every launcher-served application mints a fresh panel address at the click over the child's own session; `open` against a running launcher asks it for a panel address; applications that were serving come back after a restart (`--no-restore`). | Brobridge tests green, then `bun run check`. The smoke loads a minted panel address once. |
| 13a | `13a-intent-store-and-backlog-panel.md` | `intents.sqlite`; the plan format with a validator and a renderer; a host rule that gives a task a tier and a setting that maps a tier to a model; launcher routes; a **Backlog** panel on the rail where a person changes a task's model, removes a task or withdraws an intent. No model, nothing runs. | Tests green. The side-by-side of what knowledge and intent each needed from the tab in the report. |
| 13b | `13b-intent-capture-and-decomposition.md` | `intent.open`, `intent.task`, `intent.submit`: an analysis the host checks against the application's real routes and pages, open questions that hold the split, tasks in the plan format with host-assigned slug, tier and model; a turn that plans cannot edit; an `intent:<appId>` document on every later turn. | Tests green. One real three-part request planned by hand, in the report. |
| 13c | `13c-intent-execution.md` | `intent.start` and **Run**: each task one engineer turn on its assigned model, the executor answering the gate for edits, builds and previews of that one application, every other question put to the person in the panel, a builder that is unsure asking with `intent.ask`; completion from a verified build with one passing example per criterion; stop on a second failure with the main model's advice; stop, resume, restart interrupts. Nothing activated. | Tests green. The by-hand run's per-task table beside 08c's and 12d's numbers. |
| 13d | `13d-intent-fixups.md` | A task cannot complete by removing a finished task's example; the workspace is judged against the task's first turn; an attempt that got further earns another, up to four turns; a turn silent for eight minutes is ended. | Tests green. The rest of the `reading-list` run by hand, in the report. |
| 14a | `14a-task-context.md` | A backlog turn found by its exact run id (`task_runs`); an attempts document for every retry and every resumed task — what earlier attempts changed, how they ended, what came back, the planning model's diagnosis; a rebuildable `links` index over tasks, runs, files, cases and lessons with provenance; a task's lessons chosen by pinned, shared file, then the task's own words; `why` on the `search` event. No outcome ranking, no embeddings, no new tool. | Tests green. Six by-hand retry runs, with and without, in the report. |
| 14b | `14b-task-context-fixups.md` | The event log sanitises what it prints for every caller, with `announce` as the one line that prints a launch address whole; the task-context tests on every platform in CI; an attempt that changed nothing says what it read and the retry is told to start from an edit; a retry measured with and without its documents from fixed snapshots of a failed first attempt. | Tests green. Ten by-hand replays from two seeds, in the report. |
| 14c | `14c-refusals-and-stopping.md` | A failed task names the tool calls that were refused, to the person, the advice and the next attempt; a turn that ends with unbuilt edits is built once by the host; a second identical input refusal shows a valid input held to its schema. Then `stop` and `status` commands, **Quit** in the panel, a line when the last panel closes, and a child that exits when its launcher is gone however it died. | Tests green. The `background-remove` failure replayed on a copy of the root, in the report. |
| 14d | `14d-examples-that-can-say-it.md` | Under `match` a value may be `{"$is": "number"}` and the like, from a closed list checked when the specification is read; a route step may carry `fails`, passing only when the route refuses, by code and by words, never on a crash; a plan marks its failure criterion and the builder is told how to write its example; the attempts document's closing sentence comes out; `task-context.json` turns the attempts document and related lessons off. Run after 14c is merged. | Tests green. Four replays from 14b's edits snapshot, in the report. |
| 14e | `14e-activation-runs-the-examples.md` | Activation runs the acceptance examples on a throwaway copy of the migrated data, writes allowed and external effects refused as in a preview, so an example that writes no longer passes the preview and fails activation; the copy that becomes live is still only opened paused and asked for its health; recovery removes the throwaway copy. Run after 14d's report is committed. | Tests green. One backlog-built application activated by hand on this build and on v0.4.14, in the report. |
| 15a | `15a-a-step-refuses-what-it-does-not-know.md` | An acceptance example, each step and a step's `view` are closed objects: a misspelt `fails` or `match` is refused when the specification is read, naming the key that was meant, instead of parsing to a step that asserts nothing. Run after 14e is merged. | Tests green. Every `parseSpec` caller and what a refusal does there, in the report. |
| 15b | `15b-a-finished-example-keeps-its-words.md` | A completed task's criteria keep the hash of their examples' steps; a later task that keeps an id and changes what it says is not completed and says which. Removal refused as before; older tasks held by id only. Run after 15a. | Tests green. How often past runs changed a finished example, honest or not, in the report. |
| 15c | `15c-an-example-that-would-run.md` | The valid inputs shown after a second identical refusal are written against `examples/notes` and applied to a copy of it by a test: no `migrations/002.sql`, no `find` that matches nothing, no `…`. Run after 14e; independent of 15a and 15b. | Tests green. Every other place the engineer is taught a path that does not exist, in the report. |
| 15d | `15d-count-what-happened.md` | `editedBy` is true only for an edit that landed, so the two-turn evaluation cannot stop before the workspace changed; each step's usage is kept as it ends, a cut-short turn reports a partial subtotal, and a token mean is over the turns whose total is known, with the count. Changes `packages/broapp/src/ai/host/run.ts`. Run after 14e. | Tests green. Every published number this changes the reading of, in the report. |
| 15e | `15e-the-newest-calls-of-a-long-turn.md` | Reopens 12j's "a turn expands whole or not at all": the first turn over the cap gives its newest complete call-and-result groups under a line saying how many are not shown. Limits unchanged. Changes `run.ts`. Run after 15d. | Tests green. 12j's three measured turns through the new expansion, in the report. No completion number claimed. |
| 15f | `15f-the-same-refusal-four-times.md` | The executor counts refusals by tool and reason as the verdict groups them, and ends a backlog turn at the fourth with the workspace unchanged, telling the person, the advice and the next attempt once. Idle clock, repair limit and interactive turns unchanged. Run after 15c. | Tests green. 14c's `background-remove` replay, in the report. |
| 16a | `16a-a-preview-that-says-why.md` | From the `background-remove` run of 2026-09-18/19. A child that does not start fails `supervisor.start` with a public error carrying its reason, so `candidate.preview`, `candidate.cycle` and `launcher.previewStart` stop answering with the internal-error mask; the Candidate panel drops a start failure that is not about the candidate on screen; the `contract` topic and `SPLIT_RULES` say an `external` route runs only after activation, and a runbook line sending a person to the preview for one is refused where the effect is knowable. Gate unchanged. No change to `packages/broapp`. Run after 15f. | Tests green. Release `17470f1f`'s start failure shown in the panel with its reason, in the report. |
| 17a | `17a-what-a-run-costs-and-where-it-is.md` | A `usage` row for every turn, partial when it was cut short; a running turn's subtotal through one new optional core hook; `prices.json`, written by the person, and no cost for a model without a price; `RunProgress` with stage, limits, criteria passing and the last refusal; `launcher.overview`, one read for what needs the person, the run, spend, what is left and the applications; five more events that can raise a notification, the ones that need the person with a short generated sound, permission asked only by a click. No screen. Changes `run.ts` and `create-ai.ts`. Run after 16a. | Tests green. The route's shape as built, for 17b, in the report. |
| 17b | `17b-the-overview.md` | An Overview **screen**, a view beside the chat and the first one shown (the chat stays mounted behind it), laid out as `mockups/overview.html`: summary strip, attention band, Running now with a stage stepper, Applications, a token footer, a prices editor, and the alerts and sound controls. Drawn only in the launcher's existing `--launcher-*` variables and font, both schemes, no new token; every action opens the panel where that decision is already made. Run after 17a. | Tests green. The mockup-to-variable mapping and the by-hand look in both schemes, in the report. |
| 18a | `18a-a-model-names-its-provider.md` | Settings version 2: every provider keeps its own address and model, a version-1 file is read as it stands, and a provider is either turned on or never contacted. A model id written `<provider>:<model>`, split on the first colon, resolves to that provider with its own key and address — from a thread pin, a task override or a tier, with no store changed. Nothing falls back from one provider to another. An `openrouter()` preset. The AI SDK's `createProviderRegistry` is not used; its convention is. No screen. Changes `settings.ts`, `registry.ts`, `create-ai.ts` and the AI contract, additively. Run after 17b. | Tests green. A light task on Ollama and a standard task on the hosted model in one run, by their usage rows, in the report. |
| 18b | `18b-every-model-in-one-list.md` | `ai.modelsList` reads every enabled provider at once and lists the ones it could not reach instead of failing; `ai.providerTest`; `AiSettings` holds every provider in the build, each with its own address, key, test and switch; the conversation picker, the task model and the tier block group by provider, write qualified references, and say `on this computer` or `sent to <label>` wherever a model is chosen or named. Run after 18a. | Tests green. Both schemes by hand, the list's timing with Ollama closed, and where each tier's task ran, in the report. |

## How to run one prompt

Give the agent this exact instruction, replacing `NN`:

```
You are on the `autoapp` branch of /Users/pv/works/broapp. Confirm with
`git branch --show-current` before anything else; if it prints anything
other than `autoapp`, stop and say so.
Read prompts/autoapp/00-common-rules.md, then every file in
prompts/autoapp/reports/ in order, then prompts/autoapp/NN-*.md.
Do what NN says. Do not do anything from a later prompt.
```

## Why it is split

The agent doing the work has limited room for open-ended reasoning. Each
prompt therefore fixes every naming and layout decision up front, lists the
files to read before writing, states the exact interfaces, names the tests,
and gives the commands whose output decides whether the step is done. Where
a third-party API must be confirmed, the prompt says which `.d.ts` to open
rather than asking the agent to recall it.

## Where this came from

Three design reviews, recorded in `docs/autoapp/design.md` (written by prompt
01 from the decisions table in `00-common-rules.md`). The short version: the
contract stays the boundary; generated code runs in a child process and is
called trusted, not sandboxed, in v1; the browser never runs generated
JavaScript, it runs a pinned renderer over a declarative view specification;
every mutation from any agent passes one gate; a change becomes a candidate
release that is previewed on a data copy and then activated with a recovery
path; MCP is an adapter over the same gate, not a second door.
